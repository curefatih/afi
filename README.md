# AFI — AI gateway for your pocket

AI gateway with OpenAI-compatible API

## Quick Start 

Create your config file,

```
cp configs/example.yaml configs/myconfig.yaml
```

Run the application with your config 

```
go run cmd -config configs/myconfig.yaml
```

> for production, consider build and use the output artifact